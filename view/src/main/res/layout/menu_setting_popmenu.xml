<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@mipmap/setting_popmenu_bg_one"
    android:paddingTop="10dp">
    <!-- android:background="@drawable/setting_popmenu_bg" -->

    <com.rj.view.listview.CornerListView
        android:id="@+id/setting_list"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:cacheColorHint="#00000000"
        android:divider="#dadada"
        android:dividerHeight="1dp" />

    <!--
    <GridView
        android:id="@+id/pop_setting_list"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="#00000000"
        android:columnWidth="600dp"
        android:gravity="center"
        android:horizontalSpacing="10dp"
        android:listSelector="@drawable/popupwindo_menu_gridview_item_btn"
        android:numColumns="auto_fit"
        android:padding="5dp"
        android:stretchMode="columnWidth"
        android:verticalSpacing="10dp" />
    -->

</LinearLayout>